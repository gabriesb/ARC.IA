name: Build & Push AgentCore Runtime (ARM64)

on:
  workflow_dispatch:

env:
  AWS_REGION: ${{ secrets.AWS_REGION }}
  IMAGE_TAG: latest

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        working-directory: infra
        run: terraform init

      - name: Read ECR repository URI from Terraform
        working-directory: infra
        run: |
            if terraform output -json | jq -e '.ecr_repository_uri' > /dev/null; then
            ECR_URI=$(terraform output -raw ecr_repository_uri)
            echo "ECR_URI=$ECR_URI" >> $GITHUB_ENV
            echo "ECR repository URI detected: $ECR_URI"
            else
            echo "ERROR: Terraform output 'ecr_repository_uri' not found."
            echo "Run Terraform Apply before running the build pipeline."
            exit 1
            fi

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push ARM64 image
        run: |
          docker buildx build \
            --platform linux/arm64 \
            -t $ECR_URI:$IMAGE_TAG \
            --push .
